CREATE CACHED TABLE SAVED_GAMES(
	TURN INTEGER,
	PLAYER_A VARCHAR(255),
	PLAYER_B VARCHAR(255),
	SAVED_DATE TIMESTAMP,
	MATCHLENGTH INTEGER,
	BOARD VARCHAR(255),
	MATCHVERSION INTEGER,
	PRIMARY KEY(PLAYER_A,PLAYER_B)
)
